Allows to manage news for website.

This bundle requires database 'news' (see SQL in 'Resources/public/database' directory).

All news can define picture that is uploaded in specific folder '/userfiles/news'. You can change this path in 'Resources/config/services.yml' file.
