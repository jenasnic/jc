Allows to manage doodle (send, response...).

This bundle requires UserBundle to work (need User for response...).

It also requires database 'doodle' and 'doodleReply' (see SQL in 'Resources/public/database' directory).
